# Group experiment : Analysis Data

The raw data files are large and hence we condense simulation runs into just collision counts per iteration per group size. This data is stored in condensed_group_data.pickle in the form of a dataframe.